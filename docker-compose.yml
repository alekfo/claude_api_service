services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped

  tg-bot:
    build: .
    command: python bot.py
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
